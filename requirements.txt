# --- Core (required to run the pipeline) ---
pandas>=1.5
numpy>=1.23
scikit-learn>=1.3
matplotlib>=3.6
openpyxl>=3.1          # read .xlsx

# --- Recommended (better model + plots). Pipeline falls back gracefully if missing ---
lightgbm>=4.0          # monotone-constrained demand model (falls back to sklearn HistGBM)
seaborn>=0.12          # nicer EDA plots (falls back to matplotlib)

# --- Optional (advanced tiers; only needed if you enable them) ---
# shap>=0.43           # model interpretation
# econml>=0.14         # causal elasticity (Double ML / causal forest)  -- Tier 4
# missingno>=0.5       # missingness matrix in EDA
